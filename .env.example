# Where the "Try it" button on the Basic ADV case points.
# The intake demo is a separate app with its own backend and env: see the
# intake-demo repo. Point this at wherever you deploy it.
# Locally, `npm run dev` in intake-demo serves it on 5184.
VITE_INTAKE_URL=http://localhost:5184
